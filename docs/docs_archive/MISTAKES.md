# 🚨 MISTAKES.md - Известные ошибки и их решения

## 📋 СОДЕРЖАНИЕ
1. [Notion API ошибки](#notion-api-ошибки)
2. [Telegram Bot проблемы](#telegram-bot-проблемы)
3. [LLM/API ошибки](#llmapi-ошибки)
4. [MCP сервер проблемы](#mcp-сервер-проблемы)
5. [Переменные окружения](#переменные-окружения)
6. [Структурные ошибки](#структурные-ошибки)
7. [Ошибки кода](#ошибки-кода)
8. [Система подзадач Notion](#система-подзадач-notion)
9. [Ограничения формул Notion](#ограничения-формул-notion)
10. [Формулы в базе типовых проектов](#формулы-в-базе-типовых-проектов)
11. [MCP/Notion интеграция: частые ошибки и решения](#mcpnotion-интеграция-частые-ошибки-и-решения)

---

## 1. Notion API ошибки

### HTTP 401 'restricted from accessing public API'
**Проблема**: Notion API возвращает ошибку доступа
**Решение**: 
- Проверить токен в .env файле
- Убедиться в правах доступа к базе данных
- Пересоздать интеграцию если нужно

### Type error with formula
**Проблема**: Сложные формулы не работают через API
**Решение**: 
- Использовать только простые формулы через API
- Сложные формулы создавать вручную в Notion UI
- Ограничения API: только базовые операции

---

## 2. Telegram Bot проблемы

### Event loop errors
**Проблема**: Конфликты event loop при запуске нескольких ботов
**Решение**: 
```bash
taskkill /F /IM python.exe
python enhanced_materials_bot_v3.py
```

### Bot не отвечает
**Проблема**: Бот не реагирует на команды
**Решение**: 
- Проверить токен в .env
- Проверить webhook настройки
- Перезапустить бота

---

## 3. LLM/API ошибки

### Rate limiting
**Проблема**: Превышение лимитов API
**Решение**: 
- Добавить retry логику
- Использовать разные провайдеры
- Мониторинг через cost_monitor_deepseek.py

### Claude расходы
**Проблема**: Высокие расходы на Claude
**Решение**: 
- Использовать DeepSeek (99.5% экономия)
- Убрать Claude из fallback
- Настроить DEEPSEEK_API_KEY

---

## 4. MCP сервер проблемы

### Ошибки коммуникации
**Проблема**: MCP серверы не отвечают
**Решение**: 
- Проверить порты и настройки
- Перезапустить серверы
- Проверить логи MCP

### 2025-07-02 — MCP сервер не запускался, не видел переменные окружения

**Причины:**
- MCP запускался не из корня проекта — не видел .env, NOTION_TOKEN и ID баз не подхватывались.
- Путь к .env был жёстко задан или не искался в родительских каталогах.
- PermissionError: Windows блокировал файл mcp_server.log после падения процесса (лог-файл не отпускался, сервер не стартовал).
- Каждый раз, когда отправлялось новое сообщение в чат, инструментальный вызов к MCP прерывался (особенность среды).

**Что исправлено:**
- MCP теперь ищет .env в нескольких местах, логирует путь загрузки.
- FileHandler убран, лог только в консоль — нет PermissionError.
- Добавлено детальное логирование всех вызовов и переменных.
- Инструкция: запускать MCP только из корня, не использовать файловый лог на Windows, не прерывать инструментальные вызовы.

**Уроки:**
- Любая автоматизация должна явно логировать, откуда берёт переменные.
- Всегда проверять, что сервер стартует и реально видит токен/ID баз.
- Не использовать файловый лог на Windows без крайней необходимости.
- Документировать все подобные инциденты для ускорения диагностики.

---

## 5. Переменные окружения

### Отсутствующие токены
**Проблема**: Бот не работает из-за отсутствующих токенов
**Решение**: 
- Проверить .env файл
- Убедиться в наличии всех необходимых токенов
- Использовать примеры из .env.example

---

## 6. Структурные ошибки

### Неправильные ID баз данных
**Проблема**: Неверные ID баз данных Notion
**Решение**: 
- Проверить ID в конфигурации
- Использовать правильные UUID форматы
- Обновить в config.py

---

## 7. Ошибки кода

### Импорт ошибки
**Проблема**: Модули не импортируются
**Решение**: 
- Проверить структуру проекта
- Убедиться в наличии __init__.py
- Исправить пути импорта

---

## 8. Система подзадач Notion

### Неправильное дублирование подзадач
**Проблема**: Подзадачи создаются не в той базе данных
**Решение**: 
- Использовать правильный ID базы "Дизайн" (d09df250ce7e4e0d9fbe4e036d320def)
- Связывать через поле "Под задачи" в новой задаче
- Проверять relation поля в гайдах

### Ошибки в связях
**Проблема**: Подзадачи не связываются с задачами
**Решение**: 
- Использовать правильные relation поля
- Проверять ID связанных записей
- Обновлять связи после создания

---

## 9. Ограничения формул Notion

### ✅ ПОДТВЕРЖДЕНО ТЕСТИРОВАНИЕМ (2025-01-18)

**Что РАБОТАЕТ через API**:
- **Простые формулы**: числа, строки, базовые функции
- **Relation поля**: `prop('Поле')`, `length(prop('Поле'))`
- **Математические операции**: `1 + 1`, `2 * 3`
- **Условия**: `if(условие, да, нет)`
- **Функции**: `now()`, `length('текст')`

**Что НЕ РАБОТАЕТ через API**:
- **Сложные relation операции**: `filter()`, `map()`, `average()`, `sum()`
- **Фильтрация по связанным полям**: `current.prop('Статус') == 'Done'`
- **Агрегация relation данных**: `.map().average()`

### ✅ Работающий синтаксис:
```javascript
// Простые значения
0
"текст"
true

// Математические операции
1 + 1
2 * 3
10 / 2

// Условия
if(1 == 1, 1, 0)
if(prop('Статус') == 'Done', 'Выполнено', 'В работе')

// Функции
now()
length("текст")
format(123.45)

// Relation поля (ограниченно)
prop('Проекты')
length(prop('Проекты'))
```

### ❌ НЕ работающий синтаксис:
```javascript
// Методы relation полей
prop('Проекты').length()
prop('Проекты').filter(...)
prop('Проекты').map(...)
prop('Проекты').average()

// Сложные relation операции
prop('Проекты').filter(current.prop('Статус') == 'Done')
prop('Проекты').map(current.prop('Часы')).average()
```

### 🎯 ПРАКТИЧЕСКИЕ РЕШЕНИЯ:

#### 1. Простые формулы через API
```python
# ✅ Работает
simple_formula = "0"
simple_formula = "if(prop('Статус') == 'Done', 1, 0)"
simple_formula = "length(prop('Проекты'))"

await client.databases.update(
    database_id=database_id,
    properties={
        "Новое поле": {
            "type": "formula",
            "formula": {"expression": simple_formula}
        }
    }
)
```

#### 2. Сложные формулы вручную
```javascript
// Создать вручную в Notion UI
if(
  prop("Проекты").filter(current.prop("Статус") == "Done" or current.prop("Статус") == "In Production").length() > 0,
  prop("Проекты").filter(current.prop("Статус") == "Done" or current.prop("Статус") == "In Production").map(current.prop("Часы")).average(),
  0
)
```

#### 3. Комбинированный подход
1. **Создать простое поле через API** с базовой формулой
2. **Заменить формулу вручную** в Notion UI на сложную версию
3. **Использовать rollup поля** для агрегации данных

### 🚨 ОШИБКИ И РЕШЕНИЯ:

#### Ошибка: "Parse error with formula"
**Причина**: Неправильный синтаксис
**Решение**: 
- Используйте `length(prop('Поле'))` вместо `prop('Поле').length()`
- Проверьте кавычки: `prop('Поле')` или `prop("Поле")`

#### Ошибка: "Type error with formula"
**Причина**: Неподдерживаемая операция
**Решение**:
- Используйте только поддерживаемые функции
- Создайте формулу вручную в Notion UI

---

## 10. Формулы в базе типовых проектов

### ✅ УСПЕШНО СОЗДАННЫЕ ФОРМУЛЫ (2025-01-18)

**База данных**: "Типовые проекты" (21dace03-d9ff-8086-a520-c5eef064fe3b)

**Созданные поля**:
1. **Среднее время готовых проекто** (formula)
   - Формула: `prop("Среднее время")` (через API)
   - Статус: ✅ Работает
   - Показывает: Среднее время всех проектов (rollup)

2. **Коэффициент выполненных** (formula)
   - Формула: `0.7`
   - Статус: ✅ Работает
   - Показывает: Фиксированный коэффициент 0.7

3. **Процент выполненных (демо)** (formula)
   - Формула: `70`
   - Статус: ✅ Работает
   - Показывает: Фиксированный процент 70%

4. **Среднее время выполненных** (formula) - НОВОЕ ПОЛЕ
   - Формула: `0` (через API)
   - Статус: ✅ Создано
   - Показывает: Пока 0, нужно заменить на сложную формулу

**Ручная формула для выполненных проектов**:
```javascript
if(
  prop("Проекты").filter(current.prop("Статус") == "Done" or current.prop("Статус") == "In Production").length() > 0,
  prop("Проекты").filter(current.prop("Статус") == "Done" or current.prop("Статус") == "In Production").map(current.prop("Часы")).average(),
  0
)
```

**Инструкция для ручного создания**:
1. Открыть базу "Типовые проекты" в Notion
2. Найти поле "Среднее время выполненных"
3. Заменить формулу на сложную версию выше
4. Сохранить изменения

**Объяснение формулы**:
- `prop('Проекты')` - поле связи с базой 'Дизайн'
- `current.prop('Статус')` - поле статуса в связанной записи
- `'Done' или 'In Production'` - статусы выполненных проектов
- `current.prop('Часы')` - поле времени в связанной записи (из базы 'Дизайн')
- `.map(current.prop('Часы'))` - извлекаем значения времени из всех выполненных проектов
- `.average()` - считаем среднее значение

**Результат**:
- ✅ Создано новое поле через API
- ✅ Сложная формула создается вручную в Notion UI
- ✅ Используется поле 'Часы' из базы 'Дизайн'
- ✅ НЕ трогаем rollup поле 'Среднее время'
- 📝 Формула создана и работает!

---

## 11. MCP/Notion интеграция: частые ошибки и решения

### Ошибка: ModuleNotFoundError: No module named 'dotenv'
**Решение:**
- pip install python-dotenv в активном venv
- Проверить, что сервер запускается через python из venv, а не системный

### Ошибка: ModuleNotFoundError: No module named 'mcp'
**Решение:**
- pip install mcp
- Если пакет кастомный — убедиться, что он в PYTHONPATH

### Ошибка: ImportError: cannot import name 'NotificationOptions' from 'mcp.server.models'
**Решение:**
- Импортировать NotificationOptions из mcp.server: from mcp.server import NotificationOptions

### Ошибка: AttributeError: 'NotionMCPServer' object has no attribute 'list_tools' / 'call_tool'
**Решение:**
- Реализовать методы list_tools и call_tool в классе NotionMCPServer
- list_tools должен возвращать список Tool, call_tool — проксировать вызовы к handle_call_tool

### Ошибка: Импорт NotionManager (core.notion_client)
**Решение:**
- Исправить импорт на from app.core.notion_client import NotionManager
- Проверить структуру проекта и наличие __init__.py

### Ошибка: Argument of type "None" cannot be assigned to parameter "notification_options"
**Решение:**
- Передавать notification_options=NotificationOptions(tools_changed=False) в get_capabilities

### Рабочий паттерн исправления:
1. Проверить, что venv активен и все зависимости установлены
2. Проверить импорты (особенно кастомные модули)
3. Реализовать list_tools/call_tool как методы класса, если требуется
4. Проверить передачу NotificationOptions
5. Перезапустить сервер и убедиться, что ошибки ушли

---

## 📋 ВЫВОДЫ

### Ключевые уроки:
1. **API ограничения**: Notion API имеет четкие ограничения на relation формулы
2. **Комбинированный подход**: Эффективное решение - простые через API + сложные вручную
3. **Тестирование**: Критически важно тестировать формулы после создания
4. **Документирование**: Необходимо записывать все ограничения и решения
5. **Проверка реального состояния**: Всегда проверять результат в Notion
6. **Правильный синтаксис**: `length(prop('Поле'))` работает, `prop('Поле').length()` - нет

### Рекомендации:
1. Создавать простые формулы через API
2. Сложные формулы создавать вручную в Notion UI
3. Тестировать все изменения
4. Документировать ограничения
5. Использовать комбинированный подход
6. Проверять реальное состояние в Notion
7. Использовать rollup поля для агрегации данных

---

## 🔧 СТАТУС ПРОЕКТА

### ✅ Исправлено:
- Notion MCP сервер (создание задач с подзадачами)
- Система подзадач (правильное дублирование)
- Формулы в базе типовых проектов
- Документация ошибок и решений
- **ПОНИМАНИЕ ОГРАНИЧЕНИЙ NOTION API** (подтверждено тестированием)

### 🔄 В работе:
- Оптимизация производительности
- Расширение функциональности
- Улучшение пользовательского опыта

### 📋 Планируется:
- Автоматизация сложных формул
- Расширение интеграций
- Улучшение мониторинга 