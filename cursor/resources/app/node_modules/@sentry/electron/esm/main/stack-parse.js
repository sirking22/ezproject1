import { createGetModuleFromFilename } from '@sentry/node';
import { createStackParser, nodeStackLineParser } from '@sentry/utils';
import { app } from 'electron';

// node.js stack parser but filename normalized before parsing the module
const defaultStackParser = createStackParser(nodeStackLineParser(createGetModuleFromFilename(app.getAppPath())));

export { defaultStackParser };//# sourceMappingURL=http://go/sourcemap/sourcemaps/5b19bac7a947f54e4caa3eb7e4c5fbf832389850/node_modules/@sentry/electron/esm/main/stack-parse.js.map
