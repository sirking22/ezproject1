const core = require('@sentry/core');
const electron = require('electron');
const normalize = require('../normalize.js');

const normalizePathsIntegration = core.defineIntegration(() => {
    return {
        name: 'NormalizePaths',
        processEvent(event) {
            return normalize.normalizePaths(event, electron.app.getAppPath());
        },
    };
});

exports.normalizePathsIntegration = normalizePathsIntegration;//# sourceMappingURL=http://go/sourcemap/sourcemaps/5b19bac7a947f54e4caa3eb7e4c5fbf832389850/node_modules/@sentry/electron/main/integrations/normalize-paths.js.map
